[1mdiff --git a/backend/apps/accounts/serializers.py b/backend/apps/accounts/serializers.py[m
[1mindex f16032f..9239fa4 100644[m
[1m--- a/backend/apps/accounts/serializers.py[m
[1m+++ b/backend/apps/accounts/serializers.py[m
[36m@@ -13,11 +13,23 @@[m [mUser = get_user_model()[m
 [m
 class UserSerializer(serializers.ModelSerializer):[m
     role = serializers.CharField(source='role.name', read_only=True, default=None)[m
[32m+[m[32m    policy_accepted = serializers.SerializerMethodField()[m
[32m+[m[32m    pin_must_change = serializers.SerializerMethodField()[m
 [m
     class Meta:[m
         model = User[m
[31m-        fields = ["id", "email", "first_name", "last_name", "phone_number", "role", "is_verified"][m
[31m-        read_only_fields = ["id", "is_verified"][m
[32m+[m[32m        fields = [[m
[32m+[m[32m            "id", "email", "first_name", "last_name", "phone_number", "role",[m
[32m+[m[32m            "is_verified", "policy_accepted", "pin_must_change",[m
[32m+[m[32m        ][m
[32m+[m[32m        read_only_fields = ["id", "is_verified", "policy_accepted", "pin_must_change"][m
[32m+[m
[32m+[m[32m    def get_policy_accepted(self, obj):[m
[32m+[m[32m        return obj.policy_version == CURRENT_POLICY_VERSION[m
[32m+[m
[32m+[m[32m    def get_pin_must_change(self, obj):[m
[32m+[m[32m        profile = getattr(obj, 'employee_profile', None)[m
[32m+[m[32m        return bool(profile and profile.pin_must_change)[m
 [m
 [m
 class RegisterSerializer(serializers.ModelSerializer):[m
[1mdiff --git a/backend/apps/accounts/urls.py b/backend/apps/accounts/urls.py[m
[1mindex b1e3e38..5ebb792 100644[m
[1m--- a/backend/apps/accounts/urls.py[m
[1m+++ b/backend/apps/accounts/urls.py[m
[36m@@ -1,11 +1,12 @@[m
 from .views import UserListView[m
 from django.urls import path[m
[31m-from rest_framework_simplejwt.views import TokenRefreshView[m
 from .views import PinLoginView[m
 from .views import AcceptPolicyView[m
 from .views import ([m
     RegisterView,[m
     CustomTokenObtainPairView,[m
[32m+[m[32m    CookieTokenRefreshView,[m
[32m+[m[32m    LogoutView,[m
     MeView,[m
     PasswordResetRequestView,[m
     PasswordResetConfirmView,[m
[36m@@ -14,7 +15,8 @@[m [mfrom .views import ([m
 urlpatterns = [[m
     path("register/", RegisterView.as_view(), name="register"),[m
     path("login/", CustomTokenObtainPairView.as_view(), name="login"),[m
[31m-    path("login/refresh/", TokenRefreshView.as_view(), name="login-refresh"),[m
[32m+[m[32m    path("login/refresh/", CookieTokenRefreshView.as_view(), name="login-refresh"),[m
[32m+[m[32m    path("logout/", LogoutView.as_view(), name="logout"),[m
     path("me/", MeView.as_view(), name="me"),[m
     path("password-reset/", PasswordResetRequestView.as_view(), name="password-reset"),[m
     path("password-reset/confirm/", PasswordResetConfirmView.as_view(), name="password-reset-confirm"),[m
[1mdiff --git a/backend/apps/accounts/views.py b/backend/apps/accounts/views.py[m
[1mindex f4d3132..e1820c4 100644[m
[1m--- a/backend/apps/accounts/views.py[m
[1m+++ b/backend/apps/accounts/views.py[m
[36m@@ -1,15 +1,19 @@[m
 from .models import IPLockout, CURRENT_POLICY_VERSION[m
 from .utils import get_client_ip[m
 from .throttles import LoginRateThrottle, PasswordResetRateThrottle[m
[32m+[m[32mfrom django.conf import settings[m
 from django.contrib.auth import get_user_model[m
 from django.contrib.auth.tokens import PasswordResetTokenGenerator[m
 from django.core.mail import send_mail[m
[32m+[m[32mfrom django.middleware.csrf import get_token as get_csrf_token[m
 from django.utils.encoding import force_bytes[m
 from django.utils.http import urlsafe_base64_encode[m
 from rest_framework import generics, permissions, status, serializers[m
 from rest_framework.response import Response[m
 from rest_framework.views import APIView[m
[31m-from rest_framework_simplejwt.views import TokenObtainPairView[m
[32m+[m[32mfrom rest_framework_simplejwt.exceptions import TokenError[m
[32m+[m[32mfrom rest_framework_simplejwt.tokens import RefreshToken[m
[32m+[m[32mfrom rest_framework_simplejwt.views import TokenObtainPairView, TokenRefreshView[m
 from rest_framework import generics[m
 from apps.core.permissions import IsManagerOrAdmin[m
 from datetime import timedelta[m
[36m@@ -17,6 +21,35 @@[m [mfrom django.utils import timezone[m
 from .serializers import PinLoginSerializer[m
 [m
 [m
[32m+[m[32mdef set_auth_cookies(response, access_token, refresh_token=None):[m
[32m+[m[32m    """Attach the JWTs as httpOnly cookies. Never exposed to JS."""[m
[32m+[m[32m    cookie_kwargs = dict([m
[32m+[m[32m        httponly=True,[m
[32m+[m[32m        secure=not settings.DEBUG,[m
[32m+[m[32m        samesite='None' if not settings.DEBUG else 'Lax',[m
[32m+[m[32m    )[m
[32m+[m[32m    response.set_cookie([m
[32m+[m[32m        'access_token',[m
[32m+[m[32m        access_token,[m
[32m+[m[32m        max_age=int(settings.SIMPLE_JWT['ACCESS_TOKEN_LIFETIME'].total_seconds()),[m
[32m+[m[32m        path='/',[m
[32m+[m[32m        **cookie_kwargs,[m
[32m+[m[32m    )[m
[32m+[m[32m    if refresh_token is not None:[m
[32m+[m[32m        response.set_cookie([m
[32m+[m[32m            'refresh_token',[m
[32m+[m[32m            refresh_token,[m
[32m+[m[32m            max_age=int(settings.SIMPLE_JWT['REFRESH_TOKEN_LIFETIME'].total_seconds()),[m
[32m+[m[32m            path='/api/accounts/',[m
[32m+[m[32m            **cookie_kwargs,[m
[32m+[m[32m        )[m
[32m+[m
[32m+[m
[32m+[m[32mdef clear_auth_cookies(response):[m
[32m+[m[32m    response.delete_cookie('access_token', path='/')[m
[32m+[m[32m    response.delete_cookie('refresh_token', path='/api/accounts/')[m
[32m+[m
[32m+[m
 [m
 from .serializers import ([m
     RegisterSerializer,[m
[36m@@ -82,6 +115,11 @@[m [mclass CustomTokenObtainPairView(TokenObtainPairView):[m
         if user:[m
             user.register_password_success()[m
 [m
[32m+[m[32m        access = response.data.pop('access', None)[m
[32m+[m[32m        refresh = response.data.pop('refresh', None)[m
[32m+[m[32m        set_auth_cookies(response, access, refresh)[m
[32m+[m[32m        get_csrf_token(request)  # ensures the readable csrftoken cookie is issued[m
[32m+[m
         return response[m
 [m
 [m
[36m@@ -235,9 +273,55 @@[m [mclass PinLoginView(APIView):[m
         ip_lockout.register_success()[m
 [m
         token = CustomTokenObtainPairSerializer.get_token(employee.user)[m
[31m-        return Response({[m
[31m-            'access': str(token.access_token),[m
[31m-            'refresh': str(token),[m
[32m+[m[32m        response = Response({[m
             'pin_must_change': employee.pin_must_change,[m
             'policy_accepted': employee.user.policy_version == CURRENT_POLICY_VERSION,[m
[31m-        })[m
\ No newline at end of file[m
[32m+[m[32m        })[m
[32m+[m[32m        set_auth_cookies(response, str(token.access_token), str(token))[m
[32m+[m[32m        get_csrf_token(request)[m
[32m+[m[32m        return response[m
[32m+[m
[32m+[m
[32m+[m[32mclass CookieTokenRefreshView(TokenRefreshView):[m
[32m+[m[32m    """[m
[32m+[m[32m    Reads the refresh token from the httpOnly cookie (never from the[m
[32m+[m[32m    request body) and issues new httpOnly cookies for the rotated pair.[m
[32m+[m[32m    """[m
[32m+[m[32m    permission_classes = [permissions.AllowAny][m
[32m+[m
[32m+[m[32m    def post(self, request, *args, **kwargs):[m
[32m+[m[32m        refresh_token = request.COOKIES.get('refresh_token')[m
[32m+[m[32m        if not refresh_token:[m
[32m+[m[32m            return Response({'detail': 'Refresh token missing.'}, status=status.HTTP_401_UNAUTHORIZED)[m
[32m+[m
[32m+[m[32m        serializer = self.get_serializer(data={'refresh': refresh_token})[m
[32m+[m[32m        try:[m
[32m+[m[32m            serializer.is_valid(raise_exception=True)[m
[32m+[m[32m        except TokenError:[m
[32m+[m[32m            response = Response({'detail': 'Refresh token invalid or expired.'}, status=status.HTTP_401_UNAUTHORIZED)[m
[32m+[m[32m            clear_auth_cookies(response)[m
[32m+[m[32m            return response[m
[32m+[m
[32m+[m[32m        access = serializer.validated_data.get('access')[m
[32m+[m[32m        new_refresh = serializer.validated_data.get('refresh')  # present: ROTATE_REFRESH_TOKENS=True[m
[32m+[m
[32m+[m[32m        response = Response({'detail': 'Token refreshed.'})[m
[32m+[m[32m        set_auth_cookies(response, access, new_refresh)[m
[32m+[m[32m        return response[m
[32m+[m
[32m+[m
[32m+[m[32mclass LogoutView(APIView):[m
[32m+[m[32m    """Blacklists the refresh token (if present/valid) and clears both cookies."""[m
[32m+[m[32m    permission_classes = [permissions.AllowAny][m
[32m+[m
[32m+[m[32m    def post(self, request):[m
[32m+[m[32m        refresh_token = request.COOKIES.get('refresh_token')[m
[32m+[m[32m        if refresh_token:[m
[32m+[m[32m            try:[m
[32m+[m[32m                RefreshToken(refresh_token).blacklist()[m
[32m+[m[32m            except TokenError:[m
[32m+[m[32m                pass  # already invalid/expired — fine, we're logging out anyway[m
[32m+[m
[32m+[m[32m        response = Response({'detail': 'Logged out.'})[m
[32m+[m[32m        clear_auth_cookies(response)[m
[32m+[m[32m        return response[m
\ No newline at end of file[m
[1mdiff --git a/backend/config/settings/base.py b/backend/config/settings/base.py[m
[1mindex 89df04a..9a1d831 100644[m
[1m--- a/backend/config/settings/base.py[m
[1m+++ b/backend/config/settings/base.py[m
[36m@@ -31,6 +31,7 @@[m [mTHIRD_PARTY_APPS = [[m
     'rest_framework_simplejwt',[m
     'rest_framework_simplejwt.token_blacklist',[m
     'corsheaders',[m
[32m+[m[32m    'csp',[m
 ][m
 [m
 # Add each of our own apps here as we build it.[m
[36m@@ -52,6 +53,7 @@[m [mINSTALLED_APPS = DJANGO_APPS + THIRD_PARTY_APPS + LOCAL_APPS[m
 [m
 MIDDLEWARE = [[m
     'django.middleware.security.SecurityMiddleware',[m
[32m+[m[32m    'csp.middleware.CSPMiddleware',[m
     'corsheaders.middleware.CorsMiddleware',[m
     'django.contrib.sessions.middleware.SessionMiddleware',[m
     'django.middleware.common.CommonMiddleware',[m
[36m@@ -119,6 +121,7 @@[m [mDEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'[m
 [m
 REST_FRAMEWORK = {[m
     'DEFAULT_AUTHENTICATION_CLASSES': ([m
[32m+[m[32m        'apps.accounts.authentication.CookieJWTAuthentication',[m
         'rest_framework_simplejwt.authentication.JWTAuthentication',[m
         'rest_framework.authentication.SessionAuthentication',[m
     ),[m
[36m@@ -148,8 +151,34 @@[m [mSIMPLE_JWT = {[m
     'AUTH_HEADER_TYPES': ('Bearer',),[m
 }[m
 [m
[31m-# The React frontend (Vite) runs on a different port, so it needs CORS access.[m
[32m+[m[32m# The React frontend (Vite) runs on a different origin, so it needs CORS access.[m
 CORS_ALLOWED_ORIGINS = config('CORS_ALLOWED_ORIGINS', default='', cast=Csv())[m
[32m+[m[32m# Required for the browser to send/receive the httpOnly JWT cookies cross-origin.[m
[32m+[m[32mCORS_ALLOW_CREDENTIALS = True[m
[32m+[m
[32m+[m[32m# Django's CSRF middleware needs to know which cross-origin frontends are[m
[32m+[m[32m# allowed to make state-changing requests — same origins as CORS above.[m
[32m+[m[32mCSRF_TRUSTED_ORIGINS = config('CSRF_TRUSTED_ORIGINS', default='', cast=Csv())[m
[32m+[m
[32m+[m[32m# Render (and most PaaS hosts) terminate TLS at the proxy and forward plain[m
[32m+[m[32m# HTTP internally — without this, Django can't tell the original request[m
[32m+[m[32m# was HTTPS, which breaks SECURE_SSL_REDIRECT and secure-cookie logic.[m
[32m+[m[32mSECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')[m
[32m+[m
[32m+[m[32mSECURE_REFERRER_POLICY = 'same-origin'[m
[32m+[m[32mX_FRAME_OPTIONS = 'DENY'[m
[32m+[m
[32m+[m[32m# CSP for pages Django itself renders (admin, browsable API, password-reset[m
[32m+[m[32m# emails' linked pages if any). NOTE: this does NOT cover the React SPA —[m
[32m+[m[32m# that's served from a separate host/CDN and needs its own CSP header[m
[32m+[m[32m# (set via your hosting provider's headers config, e.g. Vercel/Netlify).[m
[32m+[m[32mCSP_DEFAULT_SRC = ("'self'",)[m
[32m+[m[32mCSP_SCRIPT_SRC = ("'self'",)[m
[32m+[m[32mCSP_STYLE_SRC = ("'self'", "'unsafe-inline'")[m
[32m+[m[32mCSP_IMG_SRC = ("'self'", "data:")[m
[32m+[m[32mCSP_FONT_SRC = ("'self'", "data:")[m
[32m+[m[32mCSP_FRAME_ANCESTORS = ("'none'",)[m
[32m+[m[32mCSP_CONNECT_SRC = ("'self'",) + tuple(CORS_ALLOWED_ORIGINS)[m
 [m
 LOGGING = {[m
     'version': 1,[m
[1mdiff --git a/backend/config/settings/production.py b/backend/config/settings/production.py[m
[1mindex 5da8ca9..8d71035 100644[m
[1m--- a/backend/config/settings/production.py[m
[1m+++ b/backend/config/settings/production.py[m
[36m@@ -36,8 +36,13 @@[m [melse:[m
 SECURE_SSL_REDIRECT = True[m
 SESSION_COOKIE_SECURE = True[m
 CSRF_COOKIE_SECURE = True[m
[32m+[m[32m# Frontend (Vercel/Netlify) and backend (Render) are different origins in[m
[32m+[m[32m# production, so cookies need SameSite=None + Secure to be sent cross-site[m
[32m+[m[32m# at all. In dev, both run on localhost (different ports only, same site),[m
[32m+[m[32m# so the base Lax default there is fine and safer.[m
[32m+[m[32mSESSION_COOKIE_SAMESITE = 'None'[m
[32m+[m[32mCSRF_COOKIE_SAMESITE = 'None'[m
 SECURE_HSTS_SECONDS = 31536000[m
 SECURE_HSTS_INCLUDE_SUBDOMAINS = True[m
 SECURE_HSTS_PRELOAD = True[m
[31m-SECURE_CONTENT_TYPE_NOSNIFF = True[m
[31m-X_FRAME_OPTIONS = 'DENY'[m
\ No newline at end of file[m
[32m+[m[32mSECURE_CONTENT_TYPE_NOSNIFF = True[m
\ No newline at end of file[m
[1mdiff --git a/backend/requirements.txt b/backend/requirements.txt[m
[1mindex 31390a7..115cad9 100644[m
[1m--- a/backend/requirements.txt[m
[1m+++ b/backend/requirements.txt[m
[36m@@ -2,6 +2,7 @@[m [masgiref==3.12.1[m
 dj-database-url==3.1.2[m
 Django==5.2.16[m
 django-cors-headers==4.3.1[m
[32m+[m[32mdjango-csp==3.8[m
 djangorestframework==3.15.1[m
 djangorestframework-simplejwt==5.3.1[m
 gunicorn==26.0.0[m
[1mdiff --git a/frontend/src/api/client.ts b/frontend/src/api/client.ts[m
[1mindex 434731c..3240fc2 100644[m
[1m--- a/frontend/src/api/client.ts[m
[1m+++ b/frontend/src/api/client.ts[m
[36m@@ -1,17 +1,34 @@[m
 import axios from 'axios';[m
 [m
 const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || 'https://security-guard-management-2elk.onrender.com/api';[m
[32m+[m
[32m+[m[32m// Tokens now live in httpOnly cookies set by the backend — never read or[m
[32m+[m[32m// written from JS. The browser attaches them automatically as long as[m
[32m+[m[32m// withCredentials is set on every request.[m
[32m+[m
[32m+[m[32mfunction getCookie(name: string): string | null {[m
[32m+[m[32m  const match = document.cookie.match(new RegExp('(?:^|; )' + name + '=([^;]*)'));[m
[32m+[m[32m  return match ? decodeURIComponent(match[1]) : null;[m
[32m+[m[32m}[m
[32m+[m
 const apiClient = axios.create({[m
   baseURL: API_BASE_URL,[m
[32m+[m[32m  withCredentials: true,[m
   headers: {[m
     'Content-Type': 'application/json',[m
   },[m
 });[m
 [m
[32m+[m[32m// CSRF double-submit: the backend sets a JS-readable "csrftoken" cookie[m
[32m+[m[32m// alongside the httpOnly auth cookies. Mirror it back as a header on every[m
[32m+[m[32m// state-changing request so Django's CSRF check passes.[m
 apiClient.interceptors.request.use((config) => {[m
[31m-  const token = localStorage.getItem('access_token');[m
[31m-  if (token) {[m
[31m-    config.headers.Authorization = `Bearer ${token}`;[m
[32m+[m[32m  const method = (config.method || 'get').toLowerCase();[m
[32m+[m[32m  if (!['get', 'head', 'options'].includes(method)) {[m
[32m+[m[32m    const csrfToken = getCookie('csrftoken');[m
[32m+[m[32m    if (csrfToken) {[m
[32m+[m[32m      config.headers['X-CSRFToken'] = csrfToken;[m
[32m+[m[32m    }[m
   }[m
   return config;[m
 });[m
[36m@@ -40,14 +57,6 @@[m [mapiClient.interceptors.response.use([m
     ) {[m
       originalRequest._retry = true;[m
 [m
[31m-      const refreshToken = localStorage.getItem('refresh_token');[m
[31m-      if (!refreshToken) {[m
[31m-        localStorage.removeItem('access_token');[m
[31m-        localStorage.removeItem('refresh_token');[m
[31m-        window.location.href = '/login';[m
[31m-        return Promise.reject(error);[m
[31m-      }[m
[31m-[m
       if (isRefreshing) {[m
         // Wait for the in-flight refresh to finish, then retry.[m
         return new Promise((resolve) => {[m
[36m@@ -57,10 +66,8 @@[m [mapiClient.interceptors.response.use([m
 [m
       isRefreshing = true;[m
       try {[m
[31m-        const { data } = await axios.post(`${API_BASE_URL}/accounts/login/refresh/`, {[m
[31m-          refresh: refreshToken,[m
[31m-        });[m
[31m-        localStorage.setItem('access_token', data.access);[m
[32m+[m[32m        // No body needed — the refresh token rides along as a cookie.[m
[32m+[m[32m        await axios.post(`${API_BASE_URL}/accounts/login/refresh/`, {}, { withCredentials: true });[m
         isRefreshing = false;[m
         refreshQueue.forEach((cb) => cb());[m
         refreshQueue = [];[m
[36m@@ -68,8 +75,6 @@[m [mapiClient.interceptors.response.use([m
       } catch (refreshError) {[m
         isRefreshing = false;[m
         refreshQueue = [];[m
[31m-        localStorage.removeItem('access_token');[m
[31m-        localStorage.removeItem('refresh_token');[m
         window.location.href = '/login';[m
         return Promise.reject(refreshError);[m
       }[m
[36m@@ -79,4 +84,4 @@[m [mapiClient.interceptors.response.use([m
   }[m
 );[m
 [m
[31m-export default apiClient;[m
\ No newline at end of file[m
[32m+[m[32mexport default apiClient;[m
[1mdiff --git a/frontend/src/features/auth/AuthContext.tsx b/frontend/src/features/auth/AuthContext.tsx[m
[1mindex 572e67b..138b56b 100644[m
[1m--- a/frontend/src/features/auth/AuthContext.tsx[m
[1m+++ b/frontend/src/features/auth/AuthContext.tsx[m
[36m@@ -1,16 +1,14 @@[m
 import { createContext, useContext, useState, useEffect } from 'react';[m
 import type { ReactNode } from 'react';[m
[31m-import { jwtDecode } from 'jwt-decode';[m
[31m-import { login as loginApi, pinLogin as pinLoginApi, acceptPolicy as acceptPolicyApi } from './authApi';[m
[32m+[m[32mimport {[m
[32m+[m[32m  login as loginApi,[m
[32m+[m[32m  pinLogin as pinLoginApi,[m
[32m+[m[32m  acceptPolicy as acceptPolicyApi,[m
[32m+[m[32m  logout as logoutApi,[m
[32m+[m[32m  fetchMe,[m
[32m+[m[32m} from './authApi';[m
 import type { LoginPayload, PinLoginPayload } from './authApi';[m
 [m
[31m-interface DecodedToken {[m
[31m-  user_id: string;[m
[31m-  email: string;[m
[31m-  role: string | null;[m
[31m-  exp: number;[m
[31m-}[m
[31m-[m
 interface AuthUser {[m
   id: string;[m
   email: string;[m
[36m@@ -26,89 +24,65 @@[m [minterface AuthContextType {[m
   pinLogin: (payload: PinLoginPayload) => Promise<void>;[m
   clearPinMustChange: () => void;[m
   acceptPolicy: () => Promise<void>;[m
[31m-  logout: () => void;[m
[32m+[m[32m  logout: () => Promise<void>;[m
   isLoading: boolean;[m
 }[m
 [m
 const AuthContext = createContext<AuthContextType | undefined>(undefined);[m
 [m
[31m-function decodeUser(token: string): AuthUser {[m
[31m-  const decoded = jwtDecode<DecodedToken>(token);[m
[31m-  return { id: decoded.user_id, email: decoded.email, role: decoded.role };[m
[31m-}[m
[31m-[m
[31m-const PIN_MUST_CHANGE_KEY = 'pin_must_change';[m
[31m-const POLICY_ACCEPTED_KEY = 'policy_accepted';[m
[31m-[m
 export function AuthProvider({ children }: { children: ReactNode }) {[m
   const [user, setUser] = useState<AuthUser | null>(null);[m
   const [isLoading, setIsLoading] = useState(true);[m
   const [pinMustChange, setPinMustChange] = useState(false);[m
   const [policyAccepted, setPolicyAccepted] = useState(false);[m
 [m
[31m-  useEffect(() => {[m
[31m-    const token = localStorage.getItem('access_token');[m
[31m-    if (token) {[m
[31m-      try {[m
[31m-        setUser(decodeUser(token));[m
[31m-        setPinMustChange(localStorage.getItem(PIN_MUST_CHANGE_KEY) === 'true');[m
[31m-        setPolicyAccepted(localStorage.getItem(POLICY_ACCEPTED_KEY) === 'true');[m
[31m-      } catch {[m
[31m-        localStorage.removeItem('access_token');[m
[31m-        localStorage.removeItem('refresh_token');[m
[31m-      }[m
[32m+[m[32m  // The JWT lives in an httpOnly cookie now, so it can't be decoded in JS.[m
[32m+[m[32m  // "Who's logged in" is instead derived by asking the backend — this runs[m
[32m+[m[32m  // on mount (page refresh) and right after login/pinLogin.[m
[32m+[m[32m  async function hydrateFromSession() {[m
[32m+[m[32m    try {[m
[32m+[m[32m      const me = await fetchMe();[m
[32m+[m[32m      setUser({ id: me.id, email: me.email, role: me.role });[m
[32m+[m[32m      setPinMustChange(me.pin_must_change);[m
[32m+[m[32m      setPolicyAccepted(me.policy_accepted);[m
[32m+[m[32m    } catch {[m
[32m+[m[32m      setUser(null);[m
[32m+[m[32m      setPinMustChange(false);[m
[32m+[m[32m      setPolicyAccepted(false);[m
     }[m
[31m-    setIsLoading(false);[m
[31m-  }, []);[m
[31m-[m
[31m-  function persistPolicyAccepted(accepted: boolean) {[m
[31m-    localStorage.setItem(POLICY_ACCEPTED_KEY, accepted ? 'true' : 'false');[m
[31m-    setPolicyAccepted(accepted);[m
   }[m
 [m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    hydrateFromSession().finally(() => setIsLoading(false));[m
[32m+[m[32m  }, []);[m
[32m+[m
   async function login(payload: LoginPayload) {[m
[31m-    const { access, refresh, policy_accepted } = await loginApi(payload);[m
[31m-    localStorage.setItem('access_token', access);[m
[31m-    localStorage.setItem('refresh_token', refresh);[m
[31m-    localStorage.removeItem(PIN_MUST_CHANGE_KEY);[m
[31m-    setPinMustChange(false);[m
[31m-    persistPolicyAccepted(policy_accepted);[m
[31m-    setUser(decodeUser(access));[m
[32m+[m[32m    await loginApi(payload);[m
[32m+[m[32m    await hydrateFromSession();[m
   }[m
 [m
   async function pinLogin(payload: PinLoginPayload) {[m
[31m-    const { access, refresh, pin_must_change, policy_accepted } = await pinLoginApi(payload);[m
[31m-    localStorage.setItem('access_token', access);[m
[31m-    localStorage.setItem('refresh_token', refresh);[m
[31m-    if (pin_must_change) {[m
[31m-      localStorage.setItem(PIN_MUST_CHANGE_KEY, 'true');[m
[31m-      setPinMustChange(true);[m
[31m-    } else {[m
[31m-      localStorage.removeItem(PIN_MUST_CHANGE_KEY);[m
[31m-      setPinMustChange(false);[m
[31m-    }[m
[31m-    persistPolicyAccepted(policy_accepted);[m
[31m-    setUser(decodeUser(access));[m
[32m+[m[32m    await pinLoginApi(payload);[m
[32m+[m[32m    await hydrateFromSession();[m
   }[m
 [m
   function clearPinMustChange() {[m
[31m-    localStorage.removeItem(PIN_MUST_CHANGE_KEY);[m
     setPinMustChange(false);[m
   }[m
 [m
   async function acceptPolicy() {[m
     await acceptPolicyApi();[m
[31m-    persistPolicyAccepted(true);[m
[32m+[m[32m    setPolicyAccepted(true);[m
   }[m
 [m
[31m-  function logout() {[m
[31m-    localStorage.removeItem('access_token');[m
[31m-    localStorage.removeItem('refresh_token');[m
[31m-    localStorage.removeItem(PIN_MUST_CHANGE_KEY);[m
[31m-    localStorage.removeItem(POLICY_ACCEPTED_KEY);[m
[31m-    setUser(null);[m
[31m-    setPinMustChange(false);[m
[31m-    setPolicyAccepted(false);[m
[32m+[m[32m  async function logout() {[m
[32m+[m[32m    try {[m
[32m+[m[32m      await logoutApi();[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      setUser(null);[m
[32m+[m[32m      setPinMustChange(false);[m
[32m+[m[32m      setPolicyAccepted(false);[m
[32m+[m[32m    }[m
   }[m
 [m
   return ([m
[36m@@ -129,4 +103,4 @@[m [mexport function useAuth() {[m
     throw new Error('useAuth must be used within an AuthProvider');[m
   }[m
   return context;[m
[31m-}[m
\ No newline at end of file[m
[32m+[m[32m}[m
[1mdiff --git a/frontend/src/features/auth/authApi.ts b/frontend/src/features/auth/authApi.ts[m
[1mindex a6eca08..9e4d90d 100644[m
[1m--- a/frontend/src/features/auth/authApi.ts[m
[1m+++ b/frontend/src/features/auth/authApi.ts[m
[36m@@ -10,9 +10,9 @@[m [mexport interface PinLoginPayload {[m
   pin: string;[m
 }[m
 [m
[32m+[m[32m// access/refresh no longer appear here — the backend sets them as httpOnly[m
[32m+[m[32m// cookies directly, so the JSON body only carries non-sensitive flags.[m
 export interface LoginResponse {[m
[31m-  access: string;[m
[31m-  refresh: string;[m
   pin_must_change?: boolean;[m
   policy_accepted: boolean;[m
 }[m
[36m@@ -21,6 +21,18 @@[m [mexport interface AcceptPolicyResponse {[m
   policy_accepted: boolean;[m
 }[m
 [m
[32m+[m[32mexport interface MeResponse {[m
[32m+[m[32m  id: string;[m
[32m+[m[32m  email: string;[m
[32m+[m[32m  first_name: string;[m
[32m+[m[32m  last_name: string;[m
[32m+[m[32m  phone_number: string;[m
[32m+[m[32m  role: string | null;[m
[32m+[m[32m  is_verified: boolean;[m
[32m+[m[32m  policy_accepted: boolean;[m
[32m+[m[32m  pin_must_change: boolean;[m
[32m+[m[32m}[m
[32m+[m
 export async function login(payload: LoginPayload): Promise<LoginResponse> {[m
   const response = await apiClient.post<LoginResponse>('/accounts/login/', payload);[m
   return response.data;[m
[36m@@ -34,4 +46,15 @@[m [mexport async function pinLogin(payload: PinLoginPayload): Promise<LoginResponse>[m
 export async function acceptPolicy(): Promise<AcceptPolicyResponse> {[m
   const response = await apiClient.post<AcceptPolicyResponse>('/accounts/accept-policy/', {});[m
   return response.data;[m
[31m-}[m
\ No newline at end of file[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mexport async function logout(): Promise<void> {[m
[32m+[m[32m  await apiClient.post('/accounts/logout/', {});[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32m// Since the JWT is httpOnly and no longer decodable in JS, this is now the[m
[32m+[m[32m// source of truth for "who is logged in" — called on mount and after login.[m
[32m+[m[32mexport async function fetchMe(): Promise<MeResponse> {[m
[32m+[m[32m  const response = await apiClient.get<MeResponse>('/accounts/me/');[m
[32m+[m[32m  return response.data;[m
[32m+[m[32m}[m
